#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
* mul - multiplies two integers
* @a: first operand
* @b: second operand
* Return: 0 if success
*/

int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
